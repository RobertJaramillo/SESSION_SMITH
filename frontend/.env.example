# MSW stays enabled by default so UI-only work remains self-contained.
# Set this to false to send /v1 requests to the FastAPI backend through Vite.
VITE_USE_MOCKS=false

# Optional when the FastAPI server is not running on its default port.
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
